[Service]
# systemd reads this root-owned file before starting or reloading Caddy.
# An interactive shell's variables are not inherited by caddy.service.
EnvironmentFile=/etc/caddy/market-sentinel.env
